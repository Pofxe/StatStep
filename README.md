# Stepik Analytics

Система для сбора и визуализации статистики курсов с платформы Stepik.

## 🏗️ Архитектура

```
┌─────────────────────────────────────────────────────────────────┐
│                     Stepik Analytics                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────┐    ┌──────────────────┐    ┌───────────┐  │
│  │  Desktop UI     │───▶│   Backend API    │───▶│ PostgreSQL │  │
│  │  (Avalonia)     │    │  (ASP.NET Core)  │    │   + EF    │  │
│  └─────────────────┘    └──────────────────┘    └───────────┘  │
│                               │                                 │
│                               ▼                                 │
│                       ┌───────────────┐                         │
│                       │   Hangfire    │                         │
│                       │   (Jobs)      │                         │
│                       └───────┬───────┘                         │
│                               │                                 │
│                               ▼                                 │
│                       ┌───────────────┐                         │
│                       │  Stepik API   │                         │
│                       │  (External)   │                         │
│                       └───────────────┘                         │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## 📊 Метрики

Система собирает и отображает:

- **Решения задач**: всего, правильные, неправильные
- **Новые ученики** (за период)
- **Выданные сертификаты**
- **Изменение репутации и знаний**
- **Количество отзывов**
- **Рейтинг курса** + дельта
- **DAU** (активные учащиеся в день)

## 🚀 Быстрый запуск

### Предварительные требования

- [.NET 10 SDK](https://dotnet.microsoft.com/download/dotnet/10.0)
- [Docker](https://www.docker.com/get-started) (для PostgreSQL)
- Stepik API credentials ([получить здесь](https://stepik.org/oauth2/applications/))

### 1. Настройка окружения

Создайте файл `.env` в папке `docker/`:

```bash
STEPIK_CLIENT_ID=your_client_id
STEPIK_CLIENT_SECRET=your_client_secret
JWT_SECRET=YourSuperSecretKeyHereMustBeAtLeast32CharactersLong!
```

### 2. Запуск Backend

```bash
# Запуск PostgreSQL
cd docker
docker compose up -d postgres

# Запуск Backend
cd ../src/StepikAnalytics.Backend
dotnet run
```

Backend будет доступен на `http://localhost:5000`

- Swagger UI: `http://localhost:5000/swagger`
- Hangfire Dashboard: `http://localhost:5000/hangfire`

### 3. Запуск Desktop приложения

```bash
cd src/StepikAnalytics.Desktop
dotnet run
```

## 📁 Структура проекта

```
/src
  /StepikAnalytics.Backend      # ASP.NET Core Web API
    /Controllers                 # REST API контроллеры
    /Services                    # Бизнес-логика
    /Stepik                      # Интеграция со Stepik API
    /Data                        # EF Core DbContext
    /Jobs                        # Hangfire задачи
    /Models                      # Доменные модели
    
  /StepikAnalytics.Desktop      # Avalonia Desktop UI
    /Views                       # XAML views
    /ViewModels                  # MVVM ViewModels
    /Services/ApiClient          # HTTP клиент к Backend
    
  /StepikAnalytics.Shared       # Общие DTO и контракты
    /Dtos                        # Data Transfer Objects
    /Contracts                   # Интерфейсы
    /Common                      # Утилиты
    
/docker
  docker-compose.yml             # Docker Compose конфигурация
```

## 🔑 API Endpoints

### Авторизация

| Метод | Endpoint | Описание |
|-------|----------|----------|
| POST | `/api/auth/login` | Вход в систему |
| POST | `/api/auth/register` | Регистрация |

### Курсы

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/api/courses` | Список курсов |
| POST | `/api/courses` | Добавить курс |
| DELETE | `/api/courses/{id}` | Удалить курс |
| POST | `/api/courses/{id}/sync` | Запустить синхронизацию |

### Метрики

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/api/courses/{id}/metrics?range=week&anchorDate=2025-01-01` | Получить метрики |

### Синхронизации

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/api/sync-runs?courseId={id}` | История синхронизаций |

## 📐 Схема БД

### courses
| Поле | Тип | Описание |
|------|-----|----------|
| id | UUID | PK |
| stepik_course_id | INT | ID курса в Stepik |
| title | TEXT | Название |
| is_enabled | BOOL | Активен ли |
| last_synced_at | TIMESTAMP | Последняя синхронизация |

### metrics_daily
| Поле | Тип | Описание |
|------|-----|----------|
| course_id | UUID | FK на courses |
| date | DATE | Дата |
| total_submissions | INT | Всего решений |
| correct_submissions | INT | Правильных |
| wrong_submissions | INT | Неправильных |
| new_learners | INT | Новых учеников |
| active_learners_dau | INT | DAU |
| rating_value | FLOAT | Рейтинг |
| ... | | |

### sync_runs
| Поле | Тип | Описание |
|------|-----|----------|
| id | UUID | PK |
| course_id | UUID | FK на courses |
| started_at | TIMESTAMP | Начало |
| finished_at | TIMESTAMP | Конец |
| status | TEXT | ok/failed |
| fetched_items_count | INT | Кол-во загруженных |

## 🔐 Безопасность

- Секреты хранятся в переменных окружения
- JWT авторизация между Desktop и Backend
- Stepik OAuth2 client credentials flow
- Polly retry policies для API запросов

## 📝 Технологии

- **Backend**: ASP.NET Core 10, Entity Framework Core, PostgreSQL
- **Desktop**: Avalonia UI 11, MVVM (CommunityToolkit), LiveCharts2
- **Background Jobs**: Hangfire
- **Logging**: Serilog
- **HTTP Client**: Polly (retry, circuit breaker)

## ⚠️ Известные ограничения

1. **Stepik API**: Некоторые метрики (reputation, knowledge delta) могут быть недоступны напрямую через API
2. **DAU**: Рассчитывается как количество уникальных пользователей с хотя бы 1 решением за день
3. **New Learners**: Оценка на основе изменения общего числа learners_count

## 📜 Лицензия

MIT
